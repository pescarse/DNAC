clone git repo
	git clone https://github.com/rofallar/hds_workshop.git
	
create python virtual environment (in project directory)
	python -m virtualenv venv_dnac
	venv_dnac\Scripts\activate.bat

add python requirments
	pip install -r requirments.txt

install helpers in Postman

Postman testing
	generate a token
	get a list of network devices
	set the device id to a switch
	get an interface list of that device

